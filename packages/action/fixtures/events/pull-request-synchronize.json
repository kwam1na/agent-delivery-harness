{
  "description": "The ordinary pull_request delivery: a pushed update to an open PR. Every scenario that does not need a special event shape is driven from this one.",
  "env": {
    "CI": "true",
    "GITHUB_ACTIONS": "true",
    "GITHUB_EVENT_NAME": "pull_request",
    "GITHUB_EVENT_PATH": "{{EVENT_PATH}}",
    "GITHUB_WORKSPACE": "{{WORKSPACE}}",
    "GITHUB_REPOSITORY": "kwam1na/agent-delivery-harness",
    "GITHUB_SHA": "{{MERGE_SHA}}",
    "GITHUB_REF": "refs/pull/42/merge"
  },
  "event": {
    "action": "synchronize",
    "number": 42,
    "pull_request": {
      "number": 42,
      "draft": false,
      "head": { "sha": "{{HEAD_SHA}}", "ref": "codex/feature" },
      "base": { "sha": "{{BASE_SHA}}", "ref": "main" }
    },
    "repository": { "full_name": "kwam1na/agent-delivery-harness", "default_branch": "main" }
  }
}
